# bootstrap.txt — configuration for the Nostr client on this page.
#
# Format: one setting per line, "key: value". Blank lines are ignored.
# Anything after a "#" is a comment. Keys are case-insensitive.
# "relay:" may be repeated — each line adds one relay, tried in the order listed.

# The author whose notes are displayed.
npub: npub1jy90jpcdl447ae3lp4924s65khdpvnttkg7fepmvmafycusyueksrvllx9

# Relays to fetch the profile (kind 0) and notes (kind 1) from.
relay: wss://nostr.wine
relay: wss://nostr.mom
relay: wss://relay.primal.net
relay: wss://xmr.usenostr.org
relay: wss://relay.mostr.pub
relay: wss://relay.ditto.pub
relay: wss://nos.lol
